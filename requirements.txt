# ─────────────────────────────────────────────────────────────
# SCP-1356 AI Server – vollständige requirements.txt
# Getestet mit Python 3.10 / 3.11 unter Ubuntu + CUDA 12.x
# ─────────────────────────────────────────────────────────────

# --- Web-Server ---
Flask==3.0.3
Werkzeug==3.0.3
gunicorn==22.0.0

# --- Numerik / Audio-Verarbeitung ---
numpy==1.26.4
scipy==1.13.1

# --- Speech-to-Text (Whisper) ---
faster-whisper==1.0.3
ctranslate2==4.5.0
av==12.3.0
huggingface_hub==0.24.6
tokenizers==0.19.1
onnxruntime-gpu==1.20.1

# --- Text-to-Speech (Piper) ---
piper-tts==1.2.0
piper-phonemize==1.1.0
onnx==1.16.2

# --- LLM (llama.cpp Bindings, wird separat mit CUDA-Flags für Blackwell/sm_120 gebaut) ---
llama-cpp-python==0.3.9

# --- cuDNN 9 Python-Fallback (falls apt-Paket cudnn9-cuda-12 nicht verfügbar ist) ---
nvidia-cudnn-cu12==9.3.0.75

# --- Hilfs- / Systembibliotheken ---
requests==2.32.3
tqdm==4.66.5
soundfile==0.12.1
pydub==0.25.1
python-dotenv==1.0.1
psutil==6.0.0

# --- Jupyter (für das "Docker-Jupyter-Ding") ---
jupyterlab==4.2.5
notebook==7.2.2
ipykernel==6.29.5
ipywidgets==8.1.5
